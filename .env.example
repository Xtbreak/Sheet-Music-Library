# Database - SQLite
DATABASE_URL="file:./dev.db"

# NextAuth
# 生成密钥: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://your-server-ip:3000"
# 是否启用安全 Cookie（HTTPS），设为 false 允许 HTTP 访问（仅限内部使用）
SECURE_COOKIE="true"
